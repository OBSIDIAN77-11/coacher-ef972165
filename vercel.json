{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ ! -d flutter-sdk ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 flutter-sdk; fi && flutter-sdk/bin/flutter --version",
  "buildCommand": "flutter-sdk/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
